#include <stdio.h>
#include <stdlib.h>
#include "cards.h"
#include "deck.h"
#include "eval.h"
#include "future.h"

int main(int argc, char ** argv) {
  future_cards_t * fc = malloc(sizeof(*fc));
  fc->n_decks = 0;

  

  exit(EXIT_SUCCESS);
}
